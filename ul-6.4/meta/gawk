#!/bin/bash -e

name=gawk
version=3.1.6
description="GNU awk"
license=GPLv3
src="g/gawk/$name-$version.tar.bz2"

showLongDescription()
{
    cat << "EOF"
The awk utility interprets a special-purpose programming language that makes it
possible to handle simple data-reformatting jobs with just a few lines of code.
EOF
}
